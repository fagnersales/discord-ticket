[build]
builder = "NIXPACKS"
buildCommand = "bun install && bun run --filter=@discord-ticket/convex build"

[deploy]
startCommand = "bun run --filter=@discord-ticket/bot start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
