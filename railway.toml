[build]
builder = "NIXPACKS"
buildCommand = "bun install && bunx turbo run build --filter=@discord-ticket/convex"

[deploy]
startCommand = "cd apps/bot && bun run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
